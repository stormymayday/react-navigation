// 1. Importing the Navigation Container
import { NavigationContainer } from "@react-navigation/native";
import Feed from "../screens/Feed";

// 2. Exporting the Main Navigation Container
export default Navigation = () => {
    return (
        <NavigationContainer>
            <Feed />
        </NavigationContainer>
    );
};
