import { View, Text, StyleSheet } from "react-native";

const Feed = () => {
    return (
        <View>
            <Text>View Screen</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default Feed;
